/**
 * Hero Block (EDS Boilerplate)
 *
 * Original boilerplate hero block, kept for reference.
 * VW pages use the custom hero-stage block instead.
 * This block has no decoration logic — it exists only to prevent
 * 404 errors if any content references the generic 'hero' block name.
 */
